import { FilterCatalogoPipe } from './filter-catalogo.pipe';

describe('FilterCatalogoPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCatalogoPipe();
    expect(pipe).toBeTruthy();
  });
});
