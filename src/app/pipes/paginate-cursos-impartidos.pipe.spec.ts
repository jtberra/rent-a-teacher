import { PaginateCursosImpartidosPipe } from './paginate-cursos-impartidos.pipe';

describe('PaginateCursosImpartidosPipe', () => {
  it('create an instance', () => {
    const pipe = new PaginateCursosImpartidosPipe();
    expect(pipe).toBeTruthy();
  });
});
