import { FiltroSolicitudesPipe } from './filtro-solicitudes.pipe';

describe('FiltroSolicitudesPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroSolicitudesPipe();
    expect(pipe).toBeTruthy();
  });
});
